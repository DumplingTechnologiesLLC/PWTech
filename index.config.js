import {
  COUNTRIES,
  US_STATES,
  REP_LOOKUP,
  CAN,
  USA,
  CAN_PROVINCES,
} from './contactData.js';

export const contactInformation = {
  contact: {
    COUNTRIES,
    US_STATES,
    CAN_PROVINCES,
    REP_LOOKUP,
    CAN,
    USA,
  },
};

export default contactInformation;
